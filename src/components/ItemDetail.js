import React from 'react'

const ItemDetail = ({item}) => {
    return (
        <div>
            <div>
            <img src={item.pictureUrl} />
            </div>

        </div>
    )
}

export default ItemDetail
